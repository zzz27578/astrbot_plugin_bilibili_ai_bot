## v1.5.0 (2026-08-21)

- 完成拟人化第一阶段基础重构：评论、B站私信与直播回复统一采用严格输出协议；只有校验后的 `reply` 可以发送，残缺 JSON、内部信号和工具参数不会外泄或提交副作用。
- 新增带关系权重的反馈候选与按话题召回。单条普通建议不会直接改变表现或人格；主人建议、多人重复或累计证据达到门槛后，才作为当前场景的聚合提醒。
- 新增永久 BV 去重账本以及 15 天详细、90 天长期、之后模糊短摘要的视频记忆生命周期；旧观看流水、缓存与语义记忆幂等迁移，不删除用户原数据。
- 视频评价改为一次返回评分理由、情绪、感想、分区、最多 5 个具体喜好信号及后续搜索词；近期/稳定偏好按证据增强、保留、减弱和删除，并支持不喜欢与审美疲劳。
- 日报与周报分离自然语言和结构化摘要；显式汇总高分视频、常见高分分区/UP、心情、关键词与反馈，周报优先组合每日摘要而不是重新灌入全部原始流水。
- 重构主动行为数量语义：浏览轮次、每轮视频、每日总观看、单视频评论和每日主动评论分别限流；最低数量与固定任务目标不再强迫执行，推荐、评论和动态均可自然跳过。
- 替换旧版每日读取原始记忆的人格演化：新版默认关闭、每周最多尝试一次，至少需要 3 个真实活动日，只更新有长度限制的近期动态区块并保留 12 个回滚快照；失败后本周不重复请求。
- 新增默认关闭的近期梗学习：候选需至少两条可核验证据，同义表达可用 Embedding 合并，达到门槛后才联网核验；总数最多 2 个、7～30 天过期，并仅低频提供给合适回复场景。
- 新增 `/bili性格回滚` 与更明确的 `/bili性格` 数据准备状态；旧 `personality_evolution.json`、配置与手动性格条目保持兼容。
- 新增 `/bili兴趣` 管理员查看入口，分开展示最近 7 天分区/UP 评分样本、具体兴趣信号、探索词与达到证据门槛的近期/稳定偏好，方便启用人格演化前人工判断。
- Web 控制台“兴趣选择与评论提示词”现在同步展示同一份视频兴趣状态；只读接口带 30 秒缓存、连续失败温和熔断、本地副本回退、限长和前端转义，不会暴露账号凭据或因偏好库异常拖慢整页。
- 当前 182 项回归测试全部通过，覆盖输出隔离、发送提交顺序、永久去重、记忆生命周期、偏好查看/演进、Web 兴趣缓存/熔断/安全限长、结构化总结、行为预算、演化门禁/回滚与梗证据约束。

## v1.4.5 (2026-08-16)

- 联网搜索新增 Firecrawl 后端，使用官方 v2 Search API，保留标题、摘要与来源链接，并兼容自建 Firecrawl Base URL。
- 联网搜索新增 Grok/xAI 后端，调用 Responses API 的原生 `web_search` 工具，自动提取最终回答与 URL 引用；默认模型为 `grok-4.6`，可自定义 API Base 与模型。
- 动态配图新增 NovelAI 官方接口兼容，支持 Persistent API Token、V4/V4.5 提示结构以及 JSON/base64、ZIP 两种图片响应。
- NovelAI 官方要求生图必须由真人操作触发，因此仅手动 `/bili动态` 会请求 NAI；定时动态选择 NAI 时自动降级为纯文字。OpenAI 兼容生图行为不变。
- 补充生图尺寸、步数、引导强度、采样器与负面提示词配置，并新增搜索响应、NAI 图片解码和真人触发限制回归测试。

## v1.4.4 (2026-08-16)

- 新增统一优先动作队列：评论、B站私信、直播回复、主动看片/看番及其点赞/投币/收藏/评论/关注/主人推荐、LLM 写工具与日周总结动态不再各自直接并发调用平台。
- 新增 SQLite 原子行为预算：全局分钟/日额度与评论、私信、直播、主动看片、动态既有硬上限共同生效，并发任务无法同时抢占最后一个名额。
- 动作持久化状态扩展为 `queued/running/succeeded/failed/unknown`；明确失败退还预算，超时或发送中断保留额度并停止盲目重试。
- 插件重启时，尚未开始发送的排队动作会安全失败并退款；已经进入平台调用的动作会隔离为“发送结果未知”。旧版 SQLite 自动补齐新字段，无需删除数据目录。
- `/bili状态` 增加队列、动作成功/失败和结果未知统计；WebUI 统计接口同步公开脱敏后的动作状态与当前预算计数。
- 修复直播回复被计入评论每日数量的问题；安全拉黑和私信 `new` 确认继续经过队列与幂等保护，但不占普通互动预算。
- 本轮不全面迁移旧提示词入口；注入包装的覆盖审计与回归测试保留到安全二期。

## v1.4.3 (2026-08-16)

- 将语义记忆正式迁入统一 SQLite 主库：首次启动自动导入现有 `memory.json`，以后写入、压缩、晋升和清理均先提交 SQLite；旧 JSON 仅作为兼容与故障恢复备份。
- 新增稳定 `rpid` 映射与原子快照替换，Embedding 从正文元数据中拆入独立向量表；升级、重复启动和清算不会重复导入同一条记忆。
- SQLite 暂时不可用时继续保留 JSON 记忆并标记待同步；恢复后自动补齐，迁移失败不会阻止评论、私信和直播事件主链启动。
- 评论、B站私信和直播召回开始真正执行有向 scope 隔离；全局语义联想不会读到其他渠道的私密对话，也不会把其他 UID 的个人记忆套到当前用户。
- 用户画像的印象、事实、标签与视频引用记录来源 scope；公开评论不再读取私信或直播形成的画像信息。
- 仅领取、过滤或忽略事件不再创建 SQLite“影子画像”，WebUI 只展示真正由成功互动提交的用户画像，避免两套画像计数分叉。
- 用户记忆压缩按 UID 与 scope 分开执行，不再把评论、私信和直播揉成同一份用户总结。
- 修复评论回复发送失败仍提前写入好感度、里程碑、画像和记忆的问题；现在与私信一致，仅在平台确认发送成功后提交副作用。
- 外部评论、B站私信和直播模型不再拥有写入全局 SELF 永久记忆的输出字段；相关信息只留在对应来源域的对话记忆与画像中。
- 记忆外部 API 升级为 v3，搜索、近期记忆和用户召回支持显式 `reader_scope`；默认采用最小可见范围。
- 新增统一记忆迁移、向量往返、兼容快照、渠道隔离、画像隔离与评论提交顺序回归测试；当前共 84 项通过。

## v1.4.2 (2026-08-16)

- 安全页将工具入口明确命名为“B站端私信回复工具”，说明它只控制B站私信回复模型，与 QQ/AstrBot 聊天工具名单分开。
- 重写周总结提示词：先从真实记录中挑选有具体依据的片段，再用第一人称写成周记；忽略无效评价、疑似错配字幕和纯计数，限制各栏目长度并减少报表腔、硬演人设与空泛收尾。
- 周总结数据增加脱敏后的评论交流摘录、主动评论和番剧近期感想，避免只有互动次数却没有可总结内容。
- 修复直播栏目标题无法被图片模板单独识别的问题；限制异常长栏目并明确省略，避免超长周报丢失末尾卡片或文字溢出统计条。
- 日总结图片改用“BiliBot 日记 / DAILY”和单日日期，不再误显示为周报。
- 新增 `ROADMAP.md`，记录直播间管理、主播语音上下文、Web 记忆管理和新手引导动效的后续安排。

## v1.4.1 (2026-08-16)

- 修复 AstrBot Pages 中由 JavaScript 动态渲染的侧边栏 Logo 路径未携带页面资源前缀或令牌、导致图片显示为破图的问题。
- 将“直播弹幕回复”界面文案明确为“进入指定 UP 主直播间参与弹幕互动”；底层行为不变，仍会监听目标直播间并由 Bot 的B站账号向同一直播间发送弹幕。
- 明确安全页中的“关注更新查询 / 关注开播查询”只供 B站私信回复模型按需调用，勾选不会创建自动巡视、进入直播间或发送弹幕。

## v1.4.0 (2026-08-16)

- 新增轻量统一事件运行时：评论、B站私信和直播弹幕统一使用平台无关的事件标识、生命周期与短期状态，为后续限流、审核和 WebUI 状态页提供稳定接口。
- 评论、私信和直播回复接入统一动作幂等保护；相同动作已成功或仍在执行时不再并发重复发送，失败动作仍可由既有恢复策略重试。
- 私信回复继续保持“平台确认发送成功后再提交副作用”，并将发送动作纳入统一运行时，避免重复回复污染好感度、画像和记忆。
- 新增 BiliBot 本体直播弹幕回复：独立低频轮询指定直播间的新弹幕，首次启动跳过历史；使用当前 AstrBot 人设、BiliBot 用户画像、直播记忆、好感度和本场最近弹幕生成短回复，并由 Bot 自己的B站账号发回直播间。
- 新增 `/bili直播 状态|房间|开始|停止|测试` 与 `/bili开关 直播回复`；支持回复冷却、每分钟上限、总字数限制、按约20字最多3条分段，以及 UID/最近发送文本双重回声过滤，避免循环回复和刷屏。
- 本体捕获的每条新直播弹幕会按 B站 UID 写入统一直播向量记忆与用户画像统计；监听和发送错误会在状态中展示，相同错误5分钟内不重复告警。
- 统一事件新增管理员、直接提及、活跃对话、感兴趣、普通和后台六档优先级；评论通知、私信待处理消息和直播弹幕合并现在使用相同规则挑选下一条事件。
- `/bili状态` 增加事件优先级统计；运行时快照提供不含消息正文、昵称和 UID 的最近事件状态，为后续审核与 WebUI 留出安全接口。
- README 补充贡献者鸣谢，并推荐可与 BiliBot 直播记忆联动的「我会直播！」插件。

## v1.3.5 (2026-08-13)

- 合并并致谢 @GuJi08233 提交的系统性代码审查修复：覆盖 Cookie/WBI、私信、原子持久化、JSON 提取、记忆排序、并发状态合并、输入消毒与多项边界处理。
- JSON 持久化改为同目录唯一临时文件，并按目标路径串行提交；修复 Windows 并发保存时的 `WinError 32/5`，序列化或替换失败时保留原文件并清理临时文件。
- 私信改为“抓取先落待处理队列、成功后确认”：处理异常或发送前失败会在 30/60 秒后有限重试，连续三次失败才进入隔离记录；进程重启或远端游标前进后仍可恢复未完成消息。
- 私信轮询改为活跃 60 秒、空闲 180 秒的自适应间隔；`-509` 仍从 10 分钟倍增退避，恢复后逐级缩短，热重载时延迟首次请求以减少反复触发频控。
- 新增可靠性回归测试，覆盖并发原子写、原文件保护、待处理消息持久化/重放、成功确认、有限重试、失败隔离及自适应轮询。

## v1.3.4 (2026-08-10)

- 修复插件删除、重装、禁用或热重载时可能遗留旧后台主循环的问题：后台任务改在 AstrBot 正式激活插件后同步启动，不再从构造函数派生无人管理的延迟任务。
- 主循环增加进程内单实例保护；再次启动会清理同事件循环中的热重载残留任务，停止插件时会等待主循环真正退出，避免多个私信轮询器互相触发 `-509`。
- 修复“某UP最近更新视频了吗”没有命中B站站内查询、误走通用联网搜索的问题；站内结果存在时不再重复联网，回复会直接依据最近投稿日期、标题和链接作答。
- 调整评论、主动评论、动态和给主人分享的默认提示词：减少服务式追问、虚构日常、固定时间开场、万能夸奖与模板化“想到你”，让生成内容更短、更具体、更自然。
- B站私信改由当前回复模型判断是否需要后台能力：可选择UP查询、视频搜索、搜索并观看或联网搜索，先自然短回应，查询完成后整合为最终回复，每轮最多向用户发送两条消息。
- 新增私信固定命令 `new`：仅当整条纯文字私信精确等于 `new` 时重置该UID的当前私信对话线程；不会清除好感度、用户画像或长期记忆。
- 私信监听改为自适应轮询：默认空闲180秒、收到消息后10分钟内60秒；`-509` 继续指数退避，恢复时逐级缩短间隔，不再立即回到高频轮询，相同退避阶段不重复输出警告；已有监听状态在插件重载后先等待一个空闲间隔，避免立即重试。

## v1.3.3 (2026-08-09)

- 修复B站私信轮询容易持续触发 `-509` 的高风险问题：默认且最短间隔改为60秒并加入随机偏移；频控退避从10分钟开始倍增至最多60分钟，连续成功3轮后才恢复。
- B站私信新增站内查询：可按自然语言搜索/推荐视频、查询UP主资料与最近投稿；明确要求Bot观看时会选择最相关搜索结果，复用视频分析并写入记忆后再回复。

## v1.3.2 (2026-08-03)

- 视频字幕改为候选信息：使用前校验是否属于当前视频，明显错配时静默忽略；不再强制“字幕冲突时以字幕为准”，并自动淘汰含字幕错配说明的旧分析缓存。
- LLM 工具由 25 个入口精简为默认 8 个：记忆、互动和番剧能力改为组合工具，UP/关注/直播查询并入 B站搜索；私信分享工具仅在开关开启时注册。
- B站私信改为独立轮询（默认30秒）；命中 `-509` 后从5分钟开始自动退避，避免持续撞频控。
- 收到允许回复用户的B站视频分享卡片时，会先执行视频分析并写入统一视频记忆，再依据真实观看结果回复；已分析视频直接复用记忆。
- Embedding 请求增加可配置超时并自动降级，向量服务超时不再阻断私信看片与回复。
- 调整B站私信及“推荐给主人”配置顺序，明确私信分享与评论区 `@主人` 的发送方式。
- 新增受控的 `watch_and_share_video_private` 工具：先完整分析视频并写入记忆，再向 `OWNER_MID` 发送B站原生视频卡片；默认关闭，带重复发送冷却且不自动重试。
- 新增 B站私信监听与回复：首次开启跳过历史，只处理纯文字和原生视频分享卡片，并复用当前人格、记忆、用户画像和好感度。
- 私信增加安全隔离：不明外链、IP 链接、疑似色情域名与成人引流不会进入 LLM，可自动拉黑；主人、Bot 自身及配置白名单始终受保护。
- 私信回复范围支持仅主人、主人和白名单、全部安全用户；默认仅主人，私信监听与真实拉黑不纳入 `/bili开关 全部`。
- 主动搜索新增最近 7 天分区评分口味摘要：按分区统计观看数和平均分，同时参与搜索词决策与高分分区筛选，仍允许自由探索。
- 推荐给主人新增 B站私信发送方式，默认私信推荐语和视频链接；评论区 @、两者都发和关闭仍可配置，并保留最低评分与每日次数限制。

## v1.3.1 (2026-07-12)

- B站视频解析新增三类独立触发开关：聊天链接自动识别、`/bili解析` 手动命令和 `bili_parse_video` LLM 工具；自动解析关闭时仍按会话暂存最近视频，之后可回复发送 `/bili解析` 或让 Bot“解析上面那个”。
- 引用消息不再直接触发自动解析；只有 `/bili解析` 或 LLM 判断用户确有解析意图时才读取引用正文。同一会话同一视频至少10分钟内不重复解析。
- 主动视频来源统一为关注、关键词搜索、视频池三路；按当天累计观看数量依优先级平均分配，分多次触发也会承接进度，来源不足时自动补位。
- B站搜索词改由带人设的 Bot 自主决定；可通过 `PROACTIVE_SEARCH_QUERY_PROMPT` 调整决策方向，也允许临时探索其他内容，模型失败时才使用分区兜底。
- 分享解析准备发送原视频时统一提示“请稍等...视频一会发出”，移除下载过程话术，随后直接发送视频或切片。
- B站分享解析新增私聊支持，并在成功解析后阻止链接继续进入默认 LLM 主链。
- 分享解析防抖改为按会话隔离，群聊与不同私聊之间不再互相影响。
- 状态页、帮助和配置说明统一更新为群聊/私聊解析。
- 新增跨插件记忆 API v2：按 B站 UID 读取画像、记录轻量视频关系，并将直播事件写入向量记忆和用户画像统计。
- 记忆 API 可直接返回今日活动总览，直播回复无需通过 Agent 反复调用 `recall_today`。
- 视频详细内容仍只进入可检索记忆；用户画像只保存 `commented_under` / `uploaded_by` / `about_user` 等轻量引用。
- 重写周总结默认提示词，减少运营周报、逐项报数、强行升华和模板化感谢语。
- 优化评论与主动评论默认提示词，强调具体回应、自然口语、避免客服腔和万能夸奖。
- @主人推荐新增最低评分与每日次数限制，推荐语要求具体说明想到对方的原因，减少营销腔和频繁打扰。

## v1.3.0 (2026-07-03)

- 新增群聊 B站分享解析：识别 BV/b23/小程序文本，发送解析卡，可选原视频切片回放。
- 新增周总结图片卡片：周总结可渲染为固定模板 PNG，QQ/B站动态优先发送图片。
- 主动视频池支持中文配置，如 `排行榜:游戏`、`最新:单机游戏`。
- 新增主动看视频前标题筛选：探索视频下载前由 LLM 判断是否想看，关注和口味分区直接放行，最多拒绝 3 次。
- 统一 `/bili日志 <视频|番剧|动态|回复>`，优化 `/bili状态`、配置项顺序和 README。

## v1.2.1 (2026-06-27)

### ✨ 新增功能

#### 💬 评论回复策略

- **概率回复**：普通评论按概率随机回复，避免有问必答显得太机械。主人、被@、高好感(熟人以上)、必回白名单 永远回复，不受概率影响
  - `REPLY_PROBABILITY_PERCENT` — 回复概率（%，默认100=每条都回）
  - `REPLY_ALWAYS_UIDS` — 必回UID白名单（不受概率/去重影响）
- **语义相似去重**：和最近回复过的评论语义高度相似的直接跳过，防复读/刷屏（需配置 Embedding）
  - `ENABLE_SIMILAR_SKIP` — 启用（默认关闭）
  - `REPLY_SIMILARITY_PERCENT` — 相似度阈值（%，默认90）

#### 🛡️ 拉黑可配 + 白名单

- 自动拉黑的阈值与次数改为可配，并加入永不拉黑白名单（主人自动包含）
  - `ENABLE_AUTO_BLOCK` — 自动拉黑总开关（默认开）
  - `BLOCK_WHITELIST_UIDS` — 永不拉黑UID白名单
  - `AUTO_BLOCK_SCORE` — 好感度低于此值自动拉黑（默认-30）
  - `AUTO_BLOCK_NEGATIVE_TIMES` — 恶意评论累计次数达标自动拉黑（默认5，0=不按次数）

### 🔧 优化

#### 🕊️ 政治/敏感话题中立

- 评论回复提示词调整：遇到政治、时政、民族宗教、领土主权等敏感话题，从「直接拒绝」改为「温和中立、不站队、不表态」，被追问/带节奏也不卷入争论

## v1.2.0 (2026-06-13)

### ✨ 新增功能

#### 📅 每周总结

- 新增周总结系统：每周固定一天自动回顾这一周的B站生活（看过的视频/番剧、发过的动态、评论区互动），用人设口吻生成总结
- 新增配置项：
  - `ENABLE_WEEKLY_SUMMARY` — 启用每周总结（默认关闭）
  - `WEEKLY_SUMMARY_MODE` — 发送方式（`qq` 私信主人 / `dynamic` 发成B站动态 / `both`）
  - `WEEKLY_SUMMARY_DAY` — 每周生成日（0=周一 … 6=周日，默认周日）
  - `WEEKLY_SUMMARY_QQ_UMO` — 接收 UMO（留空复用恶意告警的 UMO）
  - `CUSTOM_WEEKLY_INSTRUCTION` — 周总结补充提示词
- 新增 `/bili周总结` 命令手动触发
- 新增 `/biliUMO` 命令：在 QQ 私聊中发送即可获取当前 UMO，并自动写入周总结和恶意告警的 QQ UMO 配置（已有值不覆盖）
- 自动触发安排在睡眠时段、日终清算完成之后，不与清算抢资源

### 🔧 优化

#### 🏠 首页推荐视频池

- 新增视频池类型 `rcmd`（首页推荐）：从B站登录账号的首页个性化推荐中获取视频，比固定分区/热门更贴合账号兴趣
- 默认视频池从 `["popular"]` 改为 `["popular", "rcmd"]`
- 配置项 `PROACTIVE_VIDEO_POOLS` 中写 `rcmd` 即可启用

#### 🎯 口味偏好系统

- 看视频时自动记录分区（tname），高分（≥7分）且出现≥2次的分区会被识别为偏好分区
- 主动看视频时按比例分配：偏好分区视频占一半，通用池探索占一半（2个视频=1:1，3个=2:1，4个=2:2）
- 偏好数据从 watch_log 实时计算，积累越多越准，无需手动配置
- 替换了原来硬编码的 `PREFERRED_TIDS` 兜底分区列表（口味数据不足时仍使用兜底）

#### 🔗 字幕+热评联合分析

- 修复：开启视频直读/截帧分析时，字幕和热评被完全跳过的问题——现在视觉分析结果会与字幕、热评做最终联合整合（画面与字幕互相印证，冲突时以字幕为准）
- 三条分析路径（视频直读/截帧、封面视觉、纯文字）统一通过 `_build_joint_context` 接入字幕+热评
- 分析提示词加入联合分析指令：以字幕为准概括内容，结尾用1-2句描述评论区氛围/观众反响

#### 📢 推荐主人更日常

- 推荐提示词补全上下文：视频内容概括、看完的感想、推荐理由现在都会传给模型（此前只有标题，导致输出空泛营销腔）
- 明确禁止安利腔/营销腔（“快来看”“超好看”、堆感叹号、排比），要求提到视频里具体戳到的点

#### ⚙️ 配置项重排

- 全部 92 个配置项按使用逻辑重新分组排序：账号 → 人设 → 功能开关 → 回复 → 主动行为 → 特别关注 → 番剧 → 动态发布 → 周总结 → 好感度提示词 → 性格演化 → 记忆 → 视觉模型 → 联网搜索 → 图片生成 → 恶意告警 → 系统
- 修正错位项（特别关注不再插在主动行为中间，`VIDEO_SEGMENT_SEC` 归入视频视觉模型组）

## v1.1.31 (2025-05-24)

### ✨ 新增功能

#### 🎬 视频直读多格式兼容

- 新增 `VIDEO_VISION_FORMAT` 配置（下拉选择）
  - `gemini` — Gemini 原生格式（`image_url` + `video/mp4`）
  - `qwen` — 通义千问 / 硅基流动格式（`video_url` + `fps`）
  - `none` — 跳过视频直读，直接走截帧分析（默认）
- 新增 `VIDEO_VISION_FPS` 配置（Qwen 格式专用抽帧率，默认 2）
- 用户自行选择格式，不再自动猜测

#### 🛡️ 恶意告警系统

- 检测到 B站 评论区有人攻击 Bot 时，自动通过 QQ 私信通知主人
- 新增 `ABUSE_ALERT_MODE` 配置（下拉选择）
  - `off` — 关闭（默认）
  - `score` — 根据回复生成时的 `score_delta` 判断
  - `model` — `score_delta` 触发后再调一次模型二次确认，过滤误报
- 新增 `ABUSE_ALERT_QQ_UMO` — 接收通知的 QQ 私聊 UMO
- 新增 `ABUSE_ALERT_SCORE_THRESHOLD` — 触发阈值（默认 -3）
- Bot 会用自己的人设口吻向主人描述被攻击的经过（包含对方 UID），并询问是否拉黑

#### 🔨 新增 LLM 工具：B站拉黑

- 新增 `bili_block_user` 工具
  - 主人在 QQ 端说”拉黑他”等自然语言 → LLM 语义理解 → 调用工具拉黑
  - 参数：`uid`（必填）、`reason`（选填）
  - 自动调用 B站 API + 写入本地黑名单
  - 防护：不能拉黑主人

### 🔧 其他

- 版本号更新至 1.1.31

-----

## v1.1.3 (2025-05-11)

### ✨ 新增功能

#### 📺 番剧观看体系

- 新增 `/bili看番` 命令
  - 支持搜索番剧并选择具体番剧
  - 支持获取番剧详情（评分、简介、更新状态、分集信息等）
  - 支持完整观看单集内容
  - 自动获取并分析：
    - 视频字幕
    - 热评
    - 评论区讨论
    - 用户评价
  - 看完后自动生成番剧观看记忆
- 新增 QQ 工具调用：`霜序去看番`
  - 可在 QQ 聊天中直接要求 Bot 去看番
  - 当前默认只观看 1 集
- 新增 `/bili番剧记忆`
  - 查看番剧观看后的记忆存储结果
  - 用于验证番剧记忆是否正确写入

### 🔎 新增番剧查询工具

- `get_bangumi_timeline`
  - 查询最近新番时间线
  - 支持”最近有什么新番”等自然语言调用
- `get_bangumi_trending`
  - 获取热门番剧排行
  - 支持”番剧排行”等查询
- `search_bilibili(bangumi)`
  - 搜索番剧
  - 支持通过番名模糊搜索
- `get_bangumi_info`
  - 获取番剧详细信息
  - 包括评分、简介、演员、分集、播放状态等

### 🎯 追番系统

- 看完番剧后若评分 ≥ 7 分
  - 自动加入追番列表
  - 日志新增：`📌 追番成功`
- 新增 `get_bangumi_updates`
  - 查询已追番剧更新情况
  - 支持”我追的番更新了吗”等自然语言调用

### 🔧 Bug 修复

- 评论区重复回复修复
  - 新增评论去重逻辑
  - 修复部分情况下 Bot 对同一评论重复回复的问题
